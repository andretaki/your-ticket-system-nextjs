import ManageProjectsClient from '@/components/ManageProjectsClient'; // Adjust path

export default function ManageProjectsPage() {
  return (
    <div>
      {/* You can add titles or other server components here if needed */}
      <ManageProjectsClient />
    </div>
  );
} 